# Extra autostart processes
# exec-once = uwsm-app -- my-service
exec-once = ydotoold

# Disable screensaver/idle lock (retry for a while: hypridle can start later than 1s)
exec-once = for i in 1 2 3 4 5; do sleep 2; killall -q hypridle; done

# Restart waybar after env is propagated (fixes scroll not working on boot)
exec-once = sleep 2 && killall -q waybar && uwsm-app -- waybar

